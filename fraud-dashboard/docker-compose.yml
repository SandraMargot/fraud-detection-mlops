services:
  fraud-dashboard:
    build: .
    container_name: fraud-dashboard
    env_file:
      - .env
    ports:
      - "8501:8501"
    networks:
      - airflow_default

networks:
  airflow_default:
    external: true